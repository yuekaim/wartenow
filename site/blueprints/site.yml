title: Wartenau

tabs:

  dashboard:
    icon: dashboard
    columns:

      - width: 2/3
        sections:

          posts:
            extends: sections/posts
            parent: site.page('posts')

      - width: 1/3
        sections:

          pages:
            extends: sections/pages

  settings:
    icon: cog
    fields:

      categories:
        type: structure
        fields:
          name:
            type: text
