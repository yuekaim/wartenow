title: Info

options:
  changeSlug: false
  changeStatus: false
  delete: false
  duplicate: false

tabs:

  content:
    icon: text
    label: Info
    columns:

      - width: 2/3
        fields:

          text:
            label:
              en: About {{ site.title }}
              de: Über {{ site.title }}
            type: writer

      - width: 1/3
        sections:

          uploads: sections/files

  team:
    icon: smile
    label: Team
    fields:

      team:
        extends: fields/people
        label: Team
